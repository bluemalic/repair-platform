# 00 · 项目全景（进度 · 路线图 · 全链路）

> 这份文档是**入口**：5 分钟看清项目做了什么、还差什么、数据怎么流。
> 具体设计以 `docs/01~05` 为准；进度与状态的**唯一事实来源是代码与 CI**，本文件与它们不一致时以代码为准。

- 当前主线提交：以 `git log -1 --format=%h` 为准（**这里故意不写死哈希**——写死的那次，文档在创建它的同一个提交里就过期了）
- 后端测试：`mvn -B clean package` → 191/191 绿；前端：`npm run typecheck && npm run build` 通过
- CI：三个独立工作流——后端 `ci.yml`（测试 + 打包）、`frontend-ci.yml`（按 `web-admin/**` 触发）、`miniapp-ci.yml`（按 `miniapp-h5/**` 触发）

---

## 1. 一句话定位

高校后勤报修 SaaS 平台：**学生扫码报修 → 后勤派单 → 师傅接单上门 → 学生验收 → 数据自动汇总**，
三个角色（学生 / 维修工 / 后勤管理）各有独立的数据可见范围，并计划支持用自然语言直接问数（M4）。

- 学生端与维修工端：同一个 uni-app 工程（先编译 H5，备案后可编小程序），按角色路由
- 后勤管理端：独立的 Vue 3 PC 后台
- 后端：单体 Spring Boot 3 + MySQL + Redis + MinIO，一套 API 同时服务三个端

---

## 2. 当前进度

### 2.1 里程碑

| 里程碑 | 内容 | 验收标准 | 状态 |
|---|---|---|---|
| M1 | 工程骨架 + 三端账号与权限 | 三种角色登录后看到的菜单和数据不同 | ✅ |
| M2 | 工单全流程 + 状态机 + 数据权限 | 一条工单从提交走到关闭，非法跃迁被拒 | ✅ |
| M3 | 超时升级 + 统计看板 | 超时自动升级；看板有数 | ✅ |
| M4 | AI 数据助手 + SQL 安全网关 | 能问"上月各楼栋报修量"并出图；越权查询被拦 | ✅ 四层闸门 + 模型接入 + 两个接口（含 SSE 流式）+ 管理端页面；**20 问评测集 39/40 符合预期**（`docs/07`） |
| M5 | 部署上线 + CI/CD | 公网可访问，push 自动部署 | 🚧 公网已验证；CD 未启用（有意，理由见 `docs/05` §6） |
| M5 | 前端 | 三端页面可用 | ✅ 管理端 11 页 + uni-app 学生/维修工端 |

### 2.2 已实现（逐项）

**认证与权限**
- Sa-Token 账号密码登录（学号 / 工号 + 租户码）、登出、当前用户、自助改密
- **三种角色的账号生命周期**：建号（维修工单独新增 / 学生批量导入粘贴学号名单）、重置口令、停用即踢下线、
  **初始口令首登强制改密**（服务端拦截，不只是前端跳转）；账号的公共写操作收在 `AccountService` 里（ADR-011）
- RBAC：22 个权限点、4 个角色（学生 / 维修工 / 后勤管理 / 平台运营）；`@SaCheckPermission` 注解校验（`StpInterfaceImpl` 提供权限码）
- **数据权限拦截器**（ADR-002 + ADR-008）：先按租户隔离、再按角色隔离——学生只见自己的单、维修工只见负责楼栋、后勤限本租户；
  条件由 MyBatis 拦截器注入，任何 Mapper 不手写范围条件；支持带别名 JOIN（统计的联表查询已验证）

**工单域**
- 8 状态机（10 待派单 → 20 → 30 → 40 → 50 → 60；70 撤单 / 80 驳回可重新派单），非法跃迁统一拒绝
- 三端接口：提交（报修码定位）· 撤销 · 评价 / 接单 · 驳回 · 扫码到场 · 完工 / 派单 · 驳回 · 关闭
- 每次流转写 `ticket_log`（from/to/action/operator），全程可追溯；条件更新保证并发幂等
- 报修码：`repair_code` 表 + 跨端共用接口 `GET /api/tickets/by-code/{code}`（学生扫码报修与维修工扫码到场同一个）

**站内通知**
- 通知表 + 未读数 / 标记已读 / 全部已读；发送失败不影响主流程（旁路）
- 状态变更通知由**动作 → 通知规格表**统一驱动（一处定义类型 / 标题 / 接收方）
- `sendToTenantAdmins`：按角色码 ADMIN 解析接收人，用于"提醒调度方"

**超时调度**（ADR-001 / ADR-006）
- 三个节点：24h 未接单提醒 · 48h 未处理升级 · 验收超时自动关闭（唯一会改状态的超时动作）
- **基准时间统一**：未接单与未处理都按 `dispatch_time` 起算（需求口径"从派到完工"），验收超时按评价时刻起算；
  毫秒级的 ZSet 路径与分钟级的兜底扫描**必须是同一个基准**，否则两条路径会给出不同的到期时刻
- Redis ZSet 延迟队列按节点分 key，秒级消费 + 每分钟兜底扫库；`ZREM` 返回值做多实例去重
- 兜底扫描三条硬约束：**带状态过滤**（处理过的行离开结果集，N 不随时间增长）· **只取主键** · **单轮 LIMIT 200**
- 幂等靠 `ticket_log` 判重；阈值可配（`REPAIR_TIMEOUT_*_MINUTES`），通知文案按实际阈值生成

**统计看板**
- 四个接口：总览（总量 / 平均响应 / 平均处理 / 超时率 / 满意度）· 趋势（天/周，补零）· 分布（类别/楼栋/紧急度）· 师傅工作量与按时率
- 口径写在 `docs/03`；超时口径以 `ticket_log` 的超时动作为准，不重算阈值

**文件与存储（MinIO，S3 协议）**
- `POST /api/files/upload`：各端共用；只收 jpg / png / webp、单张 ≤ 5MB、按**文件头魔数**判真实类型
  （不看后缀与 Content-Type——改名的 `.html` 能被当页面打开，是存储型 XSS）
- 对象键 `租户ID/日期/uuid.扩展名`，Content-Type 由服务端写死；桶匿名可读（URL 要长期存进工单，
  预签名链接会过期）；**应用启动不依赖 MinIO**，MinIO 不可用时上传返回 `50003`、其余功能照常

**安全护栏**
- 报修码防枚举（ADR-009）：接收报修码的两个接口（按码查询 / 扫码到场）按登录用户限流，
  固定窗口 60 次/分钟，超限 `10004`；计数用 Redis + Lua 保证 TTL 一定被设置
- Redis 不可用时**放行**而不是拦死（docs/01 §5 降级策略）：限流防的是滥用，不是业务正确性

**管理端基础数据（`/api/admin`）**
- **维修工管理**：列表（分页 + 状态 / 工号姓名筛选，带负责楼栋名）· 新增（工号即登录名，
  用户类型服务端写死为维修工）· 修改启停（**停用同时踢下线**，因为登录态在 Redis，不受 status 影响）·
  设置负责楼栋（**全量替换**，校验先于删除，跨租户楼栋被拒且不动原有授权）
- **楼栋 / 类别**：列表（**不分页**，含已停用，按 sort 升序）· 新增（一律启用）· 修改（PUT 提交最终状态）·
  删除（逻辑删除，**只允许删从未被引用的**：有工单 / 有师傅负责 / 有报修码就返回 `10001`）。
  "下线一项"的正式方式是**停用**——被引用的字典项一旦软删，全局逻辑删除会让引用处静默显示空名称
- **报修码**：尚未实现（契约已在 docs/03 定好）

**移动端（miniapp-h5，uni-app）**
- 登录（学号/工号）→ 按角色分流到学生或维修工首页；后勤账号提示去管理端
- 学生侧：**手输 6 位报修码**定位房间（`by-code`）→ 选类别（类别默认紧急度自动带出）→ 描述 + 最多 3 张现场图
  （走 `POST /api/files/upload`）→ 提交 → 我的报修（下拉刷新/触底加载）→ 详情（流转时间线、维修结果、评价）→ 待派单可撤单
- 学生侧还有**验收评价**（40 → 50）：先把维修结果摆出来再打分，**星级不预选**——预选 5 星等于把"懒得点"
  的人也算成满分，满意度数据会变假
- 维修工侧：我的任务（只含负责楼栋）→ 详情 → **接单**（被抢先返回 `20003`）→ **到场打卡**（输入房间门口的
  6 位码，服务端比对位置，不一致返回 `20007`）→ **完工上报**（维修结果 + 最多 3 张照片）→ 驳回（填理由）
- 工单详情的展示部分抽成 `components/TicketDetail.vue` 两端共用（纯展示、不传函数），各自的**操作区**留在各自页面
- 全用 `uni.*`（请求 `uni.request`、上传 `uni.uploadFile`、存储 `uni.getStorageSync`），保持"将来能编小程序"（AGENTS §3.2）

**管理端前端（web-admin）**
- Vue 3 + Vite + TS + Element Plus + ECharts；**11 个页面**：登录（校验角色，含「平台运营登录」入口）· 工单列表（派单/驳回/关闭/筛选/分页）
  · **工单详情抽屉**（描述与图片、维修结果、验收评价、**流转时间线**）· **通知**（未读角标，点"查看工单"直接跳到该单详情）
  · 统计看板（4 图）· **AI 问数**（一句中文 → 表格 + 图 + 结论 + 可展开的 SQL；**流式：SQL 与数据先到，结论最后**）· 维修工管理 · **学生管理（批量导入学号名单 + 一键重置口令）** · 报修码管理 · 楼栋管理 · 类别管理
  · **平台运营的租户管理**（开通/停用学校、维护学校管理员、重置其口令；见 ADR-012）
- **派单是选人下拉**（服务端搜索启用中的维修工，选项里带"负责哪几栋楼"），不再是手填 ID
- 未读数由 `store/notification.ts` 统一持有：侧边栏角标与通知页共用一份，标记已读后马上刷新（60 秒轮询一次）
- `api/http.ts` 统一处理后端契约：`Result.code` 解包、`Long → 字符串`、401 清 token 跳登录、错误消息统一弹出
- 菜单分「工单管理 / 通知 / 统计看板 / 基础数据（五项）」；平台运营账号登录后菜单只有「租户管理」一项（两个身份看到的菜单不同，靠 `userType` 分支）

**工程侧**
- 14 张表，由 Flyway 迁移脚本建（`src/main/resources/db/migration/V1__baseline.sql`）+ 演示账号脚本 `docs/dev-seed.sql`（不进生产库）
- 配置全部 `${ENV:默认值}`，密码只从环境变量读；本地 / CI / 容器同一套读取逻辑
- Dockerfile + docker-compose + nginx（已预留 `/` 与 `/h5/` 前端静态目录）+ 部署文档 `docs/05`
- 测试 188 个：状态机流转、并发幂等、数据权限、超时调度、统计口径、鉴权链路、限流边界（报修码按用户 / 登录按账号）、接口错误契约、自助改密、首登强制改密、演示数据重置、维修工管理、学生账号管理、租户入驻与平台域收口、**AI SQL 安全网关与问数链路**、字典管理、报修码管理、文件上传

### 2.3 还没做

**P0 缺口（功能清单里的必做项）**

| 缺口 | 影响 | 备注 |
|---|---|---|
| ~~管理端基础数据接口~~ **已实现** | —— | 维修工（含负责楼栋）· 楼栋 · 类别 · 报修码 · **学生账号**：这条线已闭环，学校能自己维护账号与基础数据 |
| ~~三种角色的账号生命周期~~ **已实现** | —— | 建号（单条 / 批量导入）/ 停用即踢下线 / 重置口令 / 首登强制改密 |
| ~~租户入驻 + 后勤管理员找回口令~~ **已实现** | —— | 平台运营端（`/api/platform/**`）开通 / 停用学校、维护学校管理员、重置其口令；平台账号由环境变量引导创建。**接第二所学校的前置条件已齐**（ADR-012） |
| ~~M4 AI 数据助手 + SQL 安全网关~~ **已完成** | —— | 四层闸门 + 流式接口 + 管理端页面 + 20 问评测集（39/40 符合预期，`docs/07`）。评测还揪出两个真缺陷：子查询绕过租户注入、LEFT JOIN 注入落点错——都已修 |

**P1**：工单转派 · 统计 Excel 导出 · 操作审计日志 · 压测报告

**前端**：**管理端已完整**（11 个页面，含 AI 问数与平台运营的租户管理）；**移动端全部完成**——学生（扫码/手输报修 · 详情 · 撤单 · 验收评价）与维修工（接单 · 到场打卡 · 完工上报）。扫码两端分别实现：H5 用 `getUserMedia` + `jsqr`（要求 HTTPS 或 localhost，http 下明确提示改手输），小程序 / App 用 `uni.scanCode`；**手输 6 位码始终保留**

**部署（M5）**：**公网可访问已验证**——演示站 <http://119.91.47.63:8080>（管理端；移动端在同地址 `/h5/`）跑在 2C2G 腾讯云轻量上，五个容器，前端产物由 nginx 挂载。
仍未验证的是 **CD 的 push 自动部署**（服务器连不上 GitHub，目前是本地打包 → 上传 → 解包 → 重建）；域名与 HTTPS 等备案下来再说

**工程尾巴**：`@SecurityScheme`（文档页 Authorize 按钮）· Spotless 格式检查（CI 里注释着）· `docs/01` 里程碑勾选 ·
~~单租户假设~~ **已解决（2026-09-16）**：租户由拦截器统一注入，决策记在 ADR-008

---

## 3. 全链路图

> 本节是**文字速览**，方便在终端里直接读。**Mermaid 版**（系统上下文图 / 容器图 / 部署图 / 核心业务时序图）
> 见 `docs/06-架构图.md`；两份不一致时以本节引用的文字文档（`docs/01~05`）为准，并回去改图。

### 3.1 部署与请求链路

```
        浏览器                 浏览器 / 手机
   （后勤管理端 Vue3）      （学生 / 维修工 H5·uni-app）
          │                        │
          └───────────┬────────────┘
                      │  HTTP（HTTPS 待上线配置）
              ┌───────▼─────────────────────────────┐
              │              Nginx :80              │
              │  /            → web-admin/dist 静态 │
              │  /h5/         → miniapp-h5/dist/build/h5 │
              │  /api/**      → app:8080            │
              │  /api/ai/query/stream → 关缓冲(SSE) │
              │  /doc.html /v3/api-docs → app       │
              │  /actuator/health → app（健康检查） │
              └───────┬─────────────────────────────┘
                      │
              ┌───────▼──────────────────────────────┐
              │        Spring Boot 3 单体应用         │
              │  TraceIdFilter → SaInterceptor → 限流 │
              │  Controller → Service → Mapper        │
              │  MyBatis 插件：数据权限 → 分页         │
              │  定时任务：超时消费(1s) / 兜底(60s)    │
              └───┬───────────┬───────────┬──────────┘
                  │           │           │
            ┌─────▼────┐ ┌────▼─────┐ ┌───▼──────┐
            │ MySQL 8  │ │ Redis 7  │ │ MinIO    │
            │ 14 张表  │ │ Token /  │ │ 图片对象  │
            │          │ │ ZSet 延迟│ │（M2 后） │
            └──────────┘ └──────────┘ └──────────┘
```

### 3.2 一条工单的端到端生命周期

| # | 谁 | 动作 | 状态 | 同时发生什么 |
|---|---|---|---|---|
| 1 | 学生 | 扫码 → 提交报修 | 10 待派单 | 写 `ticket_log(SUBMIT)`；生成工单号（Redis INCR + 唯一索引兜底） |
| 2 | 后勤 | 派单给师傅 | 20 待接单 | 通知师傅；**登记 24h 未接单提醒** |
| 3 | 维修工 | 接单 | 30 处理中 | 通知学生；撤销接单提醒；**登记 48h 未处理升级** |
| 4 | 维修工 | 扫码到场打卡 | 30（不变） | 校验报修码与工单房间一致；记录 `arrive_time` / 响应时长；通知学生 |
| 5 | 维修工 | 完工上报（可传图） | 40 待验收 | 记录 `finish_time` / 处理时长；撤销处理升级；通知学生 |
| 6 | 学生 | 验收评价 | 50 已完成 | 写 `ticket_evaluation`（唯一索引防重复）；通知师傅；**登记验收超时** |
| 7 | 后勤 | 关闭工单 | 60 已关闭 | 撤销验收超时登记 |
| — | 系统 | 超时兜底 | 见下 | 24h 未接单→提醒调度方；48h 未处理→升级提醒；验收超 24h→自动关闭 |

```
10 ──派单──▶ 20 ──接单──▶ 30 ──完工──▶ 40 ──验收──▶ 50 ──关闭──▶ 60
 │            │            │            │                      ▲
 │撤单        │驳回        │驳回        │驳回                  │超时自动关(AUTO_CLOSE)
 ▼            ▼            ▼            ▼                      │
70 已撤单      └──────────── 80 已驳回 ──重新派单──▶ 20 ───────┘
```

### 3.3 鉴权、限流与数据权限（每个请求都走）

```
请求(Authorization: Bearer <token>)
   │
   ├─ TraceIdFilter       注入 traceId 到 MDC + 响应头（贯穿全链路日志）
   │
   ├─ SaInterceptor       ① 登录态校验：无 token → 401 + 10002
   │                      ② 注解校验：@SaCheckPermission("ticket:dispatch")
   │                           └─ 回调 StpInterfaceImpl：
   │                              sys_user_role → sys_role_permission → sys_permission
   │                              角色/权限码都取启用中的；失败 → 403 + 10003
   │
   ├─ RateLimitInterceptor 只对 @RateLimit 标注的接口计数（当前=接收报修码的两个接口）
   │                        Redis 固定窗口：60 次/分钟/用户，超限 → HTTP 200 + 10004
   │                        计数键 = 用户 + 接口方法（不含路径，否则每次猜码都是新桶）
   │                        Redis 不可用 → 放行（ADR-009 / docs/01 §5 降级策略）
   │
   ├─ Controller → Service（业务规则 + 状态机 + 条件更新）
   │
   └─ MyBatis 拦截器链
        ├─ DataPermissionInterceptor（ADR-002 + ADR-008，两层）
        │     TicketDataScopeHandler 先注入租户、再按角色追加：
        │       第一层 租户 → t.tenant_id = 我的租户（后勤也不例外）
        │       第二层 角色 → 后勤：不限制｜维修工：t.building_id IN (我负责的)｜学生：t.student_id = 我
        │       通知表 → receiver_id = 我（所有人一视同仁）
        │     无登录态（定时任务）→ 两层都不注入，这是兜底扫描能跨租户的前提
        └─ PaginationInnerInterceptor（必须在数据权限之后）
```

关键点：**数据范围不写在 SQL 里**（漏一处就是越权），而由拦截器统一注入；系统上下文（定时任务、无登录态）
不注入条件——这正是超时调度能跨租户处理的原因。

### 3.4 超时调度链路

```
业务流转处登记                      Redis ZSet                      调度器（1s）
派单 → registerAccept   ──▶  ticket:timeout:ACCEPT  ──┐
接单 → registerProcess  ──▶  ticket:timeout:PROCESS ──┼─▶ ZRANGEBYSCORE 0 now
评价 → registerEval     ──▶  ticket:timeout:EVAL    ──┘        │
关闭/完工/驳回 → cancel（删 member）                          ZREM（返回 1 才处理）
                                                              │
                                                     ┌────────┴────────┐
                                                     ▼                 ▼
                                          未接单/未处理：写 ticket_log  验收超时：
                                          （20→20 / 30→30）+ 通知      条件更新 50→60
                                                                      + 通知 + 写日志
兜底：每分钟扫库（"该到期却不在 ZSet 里"）→ 走同一套动作，幂等靠 ticket_log 判重
```

### 3.5 分层与包结构

```
controller/student|worker|admin   ← 只做参数接收与返回（DTO 入、VO 出，每个接口带 Knife4j 注解）
service + service/impl            ← 业务规则：状态机、幂等、事务；不写 SQL
mapper (+ resources/mapper/*.xml) ← 全部 SQL（含统计聚合）
entity / dto / vo / converter     ← 表实体 / 入参 / 出参 / 转换（禁止 entity 直接当接口出入参）
interceptor/                      ← 数据权限、鉴权
ai/                               ← M4：SQL 安全网关（**已落地**）、Schema 注入、SQL 生成（未实现）
job/                              ← 超时调度（消费 + 兜底）
common/                           ← Result / ErrorCode / BizException / 状态与动作枚举
web-admin/  miniapp-h5/           ← 两个独立前端工程（各自 package.json，见 AGENTS 3.1）
```

### 3.6 M4 目标链路（进行中，供对照）

```
自然语言问题
   │
   ├─ Schema 注入（把白名单表结构喂给模型，不喂整库）           ← **已实现**（从只读连接读 information_schema，敏感列不进提示词）
   ├─ LLM 生成 SQL（LangChain4j + DeepSeek，ADR-005）          ← **已实现**（模型层 + 编排；HTTP 接口与页面待做）
   │
   └─ SQL 安全网关（ADR-003，四层闸门，不信任模型输出）
        ① 只读账号连接（防改数据 / 删表）                      ← **已实现**（启动引导建号 + 只授白名单 SELECT；真库实测改不了数据）
        ② SQL 白名单：仅 SELECT，禁 DDL/DML/多语句              ← **已实现 + 27 条单测**
        ③ 强制租户条件（防越权读——只读账号做不到）              ← **已实现**（见下）
        ④ 超时熔断(>3s) + 返回行数上限(>1000)                  ← **已实现**（真库测了超时中断与截断标记）
              │
              └─▶ 结果 + 图表配置 + 结论                          ← **已实现**（非流式）；SSE 流式待做
```

> **第 ③ 层的实现形态与 ADR-003 的措辞有出入，以代码为准**：ADR 写的是"强制走 MyBatis 数据权限拦截器"，
> 但那个拦截器只覆盖 `ticket` / `notification` 两张表，而 AI 的 SQL 可能查白名单里**任何**一张表——
> 照字面做，`SELECT * FROM building` 会读到别家租户的楼栋。所以落地时由网关自己注入租户条件
> （复用同一套 AST 手法），覆盖全部白名单表。详见 ADR-003 的"落地时的修正"。

---

## 4. 技术栈与决策索引

| 用途 | 选型 |
|---|---|
| 后端 | Spring Boot 3.5.16 + Java 17 + MyBatis-Plus |
| 鉴权 | Sa-Token（不引 Spring Security；BCrypt 用 spring-security-crypto 工具包） |
| 存储 | MySQL 8（宿主机 13306）· Redis 7（延迟队列）· MinIO（S3 协议，待接入） |
| 文档 | Knife4j（OpenAPI 3，实现用 `knife4j-openapi3-ui` 静态包 + springdoc 2.8） |
| 前端 | 管理端 Vue 3 + Vite + Element Plus + ECharts；移动端 uni-app |
| AI（M4） | LangChain4j + DeepSeek API（不用它的 Spring Boot starter） |
| 部署 | Docker + Compose + Nginx；CI/CD 用 GitHub Actions |

| ADR | 决策 | 落地状态 |
|---|---|---|
| 001 | Redis ZSet 延迟队列替代定时轮询 | ✅ 已实现 |
| 002 | MyBatis 拦截器做数据权限 | ✅ 已实现 |
| 003 | AI 生成的 SQL 必须过安全网关 + 数据权限 | ✅ 已实现（含评测后的两条修正） |
| 004 | 报修码用「位置码 + 映射表」 | ✅ 已实现 |
| 005 | AI 框架选 LangChain4j，不引 starter | ✅ 已实现 |
| 006 | 超时按节点共用一套 ZSet；统计不建表、口径同源 | ✅ 已实现 |
| 007 | 前端先单仓，守住边界保留拆分路径 | 🚧 部分（脚手架就绪） |
| 008 | 租户与角色两层范围条件收敛到同一个拦截器 | ✅ 已实现 |
| 009 | 报修码接口限流：Redis 固定窗口 + 注解，故障时放行 | ✅ 已实现 |
| 010 | 表结构交给 Flyway 管 | ✅ 已实现 |
| 011 | 学生账号统一初始口令 + 首登强制改密 | ✅ 已实现 |
| 012 | 平台运营用「租户 0 + user_type=4」，域收口靠拦截器 | ✅ 已实现 |
| 013 | 流式问数只流阶段、用 fetch 不用 EventSource | ✅ 已实现 |

---

## 5. 关键约定速查

- **状态码**：10 待派单 · 20 待接单 · 30 处理中 · 40 待验收 · 50 已完成 · 60 已关闭 · 70 已撤单 · 80 已驳回
- **错误码段**：1xxxx 通用 · 2xxxx 工单 · 3xxxx 权限/账号 · 4xxxx AI · 5xxxx 文件（详见 `docs/03`）
- **超时阈值**：`REPAIR_TIMEOUT_ACCEPT_MINUTES`(1440) · `PROCESS_MINUTES`(2880) · `EVAL_MINUTES`(1440)
- **通知类型**：`TICKET_DISPATCHED/ACCEPTED/ARRIVED/FINISHED/EVALUATED/REJECTED/AUTO_CLOSED/ACCEPT_TIMEOUT/PROCESS_TIMEOUT`
- **JSON 契约**：`Long` 一律序列化成字符串（雪花 ID 精度）；分页计数字段保持数字（前端分页组件要求）
- **提交规范**：Conventional Commits；前端 scope 固定 `admin` / `h5`（将来切分历史的分界线）
- **数据可见范围**：学生=本人 · 维修工=负责楼栋 · 后勤=本租户全部

---

## 6. 本地怎么跑

```bash
docker compose up -d mysql redis          # 中间件（表结构与基础数据由应用启动时的 Flyway 建立）
# 需要账号时执行一次 docs/dev-seed.sql（admin / worker01 / 20260001，口令 Repair@2026）

mvn -B clean package                       # 后端：测试 + 打包
java -jar target/repair-platform-0.0.1-SNAPSHOT.jar   # 起后端（8080）

cd web-admin && npm install && npm run dev # 管理端（5173，/api 代理到 8080）
```

`http://localhost:8080/doc.html` 是接口文档；环境变量与部署细节见 `docs/05-部署文档.md`。

---

## 7. 文档索引

| 文件 | 内容 |
|---|---|
| `README.md` | 项目介绍 + 进度表 + 快速开始 |
| `AGENTS.md` | 硬性约定：技术栈 / 包结构 / 前端边界 / 编码与提交规范 / 明确不做清单 |
| `docs/01-需求文档.md` | 需求、角色、P0/P1、里程碑与验收标准 |
| `docs/02-数据库设计.md` | 14 张表、索引、状态机定义 |
| `docs/03-接口规范.md` | 全部接口、错误码表、统计口径 |
| `docs/04-架构决策记录.md` | ADR-001 ~ 009（含各自落地状态） |
| `docs/05-部署文档.md` | 部署拓扑、Compose、Nginx、上线步骤 |
| `docs/06-架构图.md` | 系统上下文 / 容器 / 部署 / 核心业务时序（Mermaid；每张图标注事实来源） |
| `docs/07-AI评测集.md` | AI 问数的 20 问评测集：准确率、错例、评测发现的问题（会花钱，手动跑） |
| `src/main/resources/db/migration/` | **表结构与基础数据的执行真相**（Flyway 迁移脚本） |
| `docs/dev-seed.sql` | 演示账号脚本（不进生产库） |
